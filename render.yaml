services:
  - type: web
    name: mai-prime-api
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
      - key: NODE_VERSION
        value: 20.0.0
