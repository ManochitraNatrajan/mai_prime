{
  "name": "finance-attendance",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-all": "npm install --prefix frontend --include=dev && npm install --prefix backend",
    "build": "npm run install-all && npm run build --prefix frontend",
    "postinstall": "npm run install-all",
    "start": "node backend/index.js",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"node backend/index.js\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jimp": "^1.6.1"
  }
}
